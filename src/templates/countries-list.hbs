<ul class="countries-list">
    {{#each this}}
    <li class="countries-list__item">
        <span class="country-name">{{this.name}}</span>
        <img class="country-flag" src="{{this.flag}}" alt="country flag"
            width="40" height="30">
    </li>
    {{/each}}
</ul>